#!/usr/bin/env bash

set -o errexit

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

xrandr --output eDP1 --auto
xrandr --output DP2 --off
"${SCRIPT_DIR}/screensize-change"
