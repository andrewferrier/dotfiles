#!/usr/bin/env bash

set -o errexit
set -o noglob
set -o nounset
set -o pipefail

shopt -s inherit_errexit

file="$1"

echo "$file" |
    entr -r sh -c "latexmk -pdf -interaction=nonstopmode -halt-on-error '$file'"
